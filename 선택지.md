# D1 — walk-speed Stat의 단일 위치 + Components::Movement의 운명
* (b) Components::Movement 완전 삭제 ← 권고
변화 예상: MovementComponents.h 삭제 + CreatePlayerActor의 else(physics 미사용) 분기 제거 + CMake에서 관련 제거. IMovable 구현체 = PhysicsMovement 단일.
장점: 단일 이동 기구 (혼란 제거), dead 분기 제거.
단점: 비물리 액터 영영 불가 → 미래에 필요하면 재추가.
* 내 의견 
    Impluse 관련 컴포넌트로 엮을떄, 
    동일한 물리 컴포넌트를 사용한다는 점에서 
    일관성이 있어 보이기도 한다.


# D2 — 신규 Impulse 위치/인터페이스
* mHitInvincibility는 Life에 넣어야 한다.
* Physics/physics_impulse.h + 신규 IImpulsabl에 넣는게 맞는거 같다.
  그리고 PhysicsMovement가 레퍼런싱하는 b2Body를 사용해야한다.
(a) Physics/physics_impulse.h + 신규 IImpulsable, i-frame→Life ← 권고
변화 예상: physics_impulse.h 신규(b2Body 직접 조작, FindPhysics로 body 해소). Components.Interfaces.h에 IImpulsable{DoImpulse(vec2)} 추가. i-frame은 D5/Life로.
장점: PhysicsMovement와 위치 일관(b2Body 쓰는 건 Physics/). Entity 계층에 Box2D 누수 없음(clean-arch). i-frame이 올바른 곳(Life).
단점: 사실상 없음.

# D3 — PlayerSpriteDirector anim API 범위 + EFacing 구동원

*  설명: 자산이 방향별 IDLE/MOVE만 있고 Attack/Hit/Die 스프라이트는 없습니다. -> 의도된것.
Attack, Hit, Die에는 Sprite가 없다.
한다면, 방향별 Idle를 재활용한다.
* "① 애니 트리거 API를 노출할지" 이것은 무슨말인지 잘 이해가 안되어 더 자세한 설명을 해주길 바란다. API를 노출 안하면? API를 노출하면? 어떻게 설계가 달라지는지?
* 이동 속도(velocity)로 구동하고 이동중에 공격이면 공격방향(aim)의 우선순위가 더 강하다.

# D4 — Weapon이 attackDir 저장? + IAttackable 채택?
* 그런데 조준은 이미 PlayerController::UpdateAim이 단일 권위 -> 정답. 인풋에 의존하는 벡터는 반드시 PlayerController 책임이 되야한다. Weapon에 Dir를 저장하는것은 올바르지 못하다.
* PlayerController에서 람다함수를 `XXX(vmath::vec2 forwardDir)` 형식의 함수를 바인딩 해야한다.
* (a) per-shot 수신, 저장 안 함, IAttackable 미채택 ← 권고
변화 예상: Weapon 거의 무변경. PB.Attack/mAttackDir 순삭제. BulletSpawnPlayable.{h,cpp} 삭제(유일 소비자였음).
장점: aim 단일 진실(PlayerController). 변경 0.
단점: 없음.

# D5 — EnemyContactHandler → Life::DoDamaged repoint
* (a) Life::DoDamaged로 repoint, PlayerBehavior.h include 제거 ← 권고
변화 예상: EnemyContactHandler.cpp:13-14를 GetComponent<Life>()->DoDamaged(mDamage)로. i-frame 게이트가 Life에 들어가 총알+접촉 모두 보호.
장점: 접촉 데미지 비로소 작동(load-bearing 수정). i-frame 보편.
단점: 없음. (부수결정: Die() 처리 방식 — 아래.)
* Life::DoDamaged는 Player든, Enemy든 공통으로 가질 수 있는 컴포넌트인게 맞다.
* 그리고 Projectile든 상위 객체인 어떤 추상화된 객체든
특정 컴포넌트를 가지는 b2Body 기반 객체에게 영향을 가하든, 데이터를 전달하든 등등 작업을 해야하니
b2Body를 가진 Non-Entity Data Transfer 일명 나는 Carrier라고 표현하고 일명 다른 Entity 충돌체와 상호작용하는 편지지의 역할릉 염두하고 제작해야한다.


----

#1 이벤트 등록 연출 (OnXXX + 약한 참조)
* 아 여기서 또 추가로 변경점이 있는데.
Entity Compoenet 시리즈 같으 추상적인 위치의 클래스를 상속받아서
EnemyXXXCompoenent, PlayerXXXCompoenent 등등..
(예, PlayerLifeCompoenet, EnemyMoveCompoenet)로 살짝 조금더 구체적인 샌드박스 상속으로 각 만들고,
functional 이벤트 기반 보단
여기서 `OnDamaged, OnUseActack` 을 virtual abstrcat 함수로 만들어 구현하든지, 아니면 OnXXX를 구현해야 하는 Interface를 만들어 다중 상속을 해야할지 고민된다.
즉, 말하고 싶은것은 functional을 통한 이벤트 등록을 회피하고 싶다는거야.

#2 Carrier 설계 (나) — "Entity는 서로 직접 호출 안 한다, Carrier가 배달"
* Q-C1 배달 경유: 인터페이스(IDamageable/IImpulsable) 경유 확정? -> 확정 동의한다 
* Q-C2 형태 : base+subtypes(Projectile/ContactCarrier)로
* Q-C3 EnemyContactHandler 처리: (a) Carrier로 흡수/대체(권고 — 동일 일 함) vs (b) 유지하되 Carrier로 리네임. + 몸통박치기 = 적 자식 센서 Carrier(늘 둘러쌈) 구조 동의? -> 동의한다.
* Q-C4 연출 분리: A2의 SetOnHitFx가 Carrier로 흡수됨 -> 동의

---


Carrier 배달 : ForEachComponent + dynamic_cast<IDamageable*> — 진짜 인터페이스 배달
* 그런데 GetCompoenet를 Interface에 접근하는 모범적인 게임 엔진 방법은 더 없는것인가?
결국 ForeachCompoenet로 모두 찾는것은 말이 되는것은 동의한다.그런데 다른 방법은 더 없는것인지
그것을 고민해줘  @.agents/skills/clean-ddd-hexagonal/SKILL.md 를 통해
그렇게 함으로써 RD1을 해결할 수 있을것 같은데