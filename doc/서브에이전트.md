You are implementing **Part D — `EnemyEntity` facade** (small) of an "Entity Accessor-facade" refactor. C++17/CMake/Box2D top-down shooter.

Work from: `/Users/escatrgot/DevelopProjects/SSU/GlobalMedia-OpenGL-ComputerGraphics`

## Project guardrails (MUST follow)
- **검증 = 빌드만**: `cmake --build --preset ninja --target _MyApp_` → exit 0. **단위테스트 금지**(`no_auto_tests`). TDD 안 함.
- **커밋 절대 금지**: working tree 에만 남김. `git add`/`git commit` 금지.
- 주석 **한국어**. 헤더가드 `#pragma once` 금지(`__..._H__` 형식). `Co-Authored-By` 미사용.
- **미접근**: `main.cpp`, `HUD/*`, `Bootstrap/EnemyBuilder.cpp`, `src/material/pass.h`, `src/timer/*`, 셰이더. (이번 Part 무관.)
- clangd Information-level missing-includes 진단 무시(빌드 무관).

## Context
- **Part B/C 완료(working tree, uncommitted)**: `apps/_MyApp_/src/Entity/BaseEntity.{h,cpp}` 공유 facade(`class BaseEntity : Component, ILivable, IDieable, IDamageable, IImpulsable` — OnEnter 4캐시 Life/Physics/Director/Impulse + 위임/accessor). `PlayerEntity : BaseEntity` 도 정착됨.
- 적도 `BaseEntity` 파생 facade 를 부착해 Life/Physics/Director/Impulse 접근 통합. 적은 추가 캐시가 필요 없어 **베이스만으로 충분**(header-only).
- `apps/_MyApp_/src/Entity/Enemy/EnemyFactory.h` 의 `CreateEnemyActor(const EnemyConfig&)` (namespace `TopdownShooter::Entity::Enemy`) 는 현재: CircleBody(BodyConfig) 부착 → Life → SimplePursueAI(playerTarget, pb->GetBody(), speed) → ContactCarrier → (조건부)EnemyDeathHandler → `return actor;`.

## Task Description (정확히 이대로)

### D1-a: `apps/_MyApp_/src/Entity/Enemy/EnemyEntity.h` 신규 (header-only)

```cpp
#ifndef __TOPDOWNSHOOTER_ENTITY_ENEMY_ENEMY_ENTITY_H__
#define __TOPDOWNSHOOTER_ENTITY_ENEMY_ENEMY_ENTITY_H__

#include "Entity/BaseEntity.h"

namespace TopdownShooter::Entity::Enemy
{
	/// @brief 적 Accessor-facade — BaseEntity(Life/Physics/Director/Impulse) 만으로 충분.
	///        (적 전용 능력 생기면 여기 확장.) header-only — OnEnter 는 BaseEntity 상속.
	class EnemyEntity : public TopdownShooter::Entity::BaseEntity
	{
	};
} // namespace TopdownShooter::Entity::Enemy

#endif // __TOPDOWNSHOOTER_ENTITY_ENEMY_ENEMY_ENTITY_H__
```

### D1-b: `apps/_MyApp_/src/Entity/Enemy/EnemyFactory.h` 부착
- include 블록에 `#include "Entity/Enemy/EnemyEntity.h"` 추가.
- `return actor;` **직전**에:
```cpp
        actor->AddComponent<EnemyEntity>();
```
- ⚠ `target_link_libraries`/CMake 변경 불필요(EnemyEntity 는 header-only — Entity/CMakeLists.txt 소스 추가도 불필요). 만약 부착으로 인해 빌드/링크 문제가 나면 보고.

## Build & verify
```
cmake --build --preset ninja --target _MyApp_
```
exit 0. 허용 경고 = sb7 `gl.h and gl3.h` `#warning`(+기존 imgui 경고). 그 외 0.

## Before you begin
EnemyFactory.h 의 현재 실제 구조(include/return 위치)가 위 가정과 다르면 시작 전 보고.

## Your job
1. D1-a/D1-b 구현. 2. 단위테스트 금지·빌드로만 검증. 3. **커밋 금지**. 4. self-review. 5. 보고.

## Report format
- **Status:** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- 구현 / 빌드 결과(exit + 마지막 링크 줄) / 변경·신규 파일 / self-review / 우려.