### Death Handler 삭제해야 함. 
1. Update로 Life Polling 하면 안됨
2. Death Handler Life에넣어야 한다
3. 추가로 디졸브도 자연스럽게 연결하고 물리 트리거도 바로 꺼야하는등 EnemyENtity를 구성하는 컴포넌트 캐시들과도 통신해야 한다.

```
void DoDie() override
{
if (mDeathFxFired) return;               // one-shot
mDeathFxFired = true;
const vmath::vec3 pos = GetOwner() ? GetOwner()->GetTransform().Translate : vmath::vec3(0.0f);
if (mSink) mSink->ReactDied(pos);        // 디졸브 시작 (sink 가 구동 — 분해 Task 6)
if (mOnDeathFx) mOnDeathFx(pos);         // spawn-at-point seam
if (mDieTimer)
	mDieTimer->Reset();                  // 지연 발동 — Update 가 만료 시 비활성 (mDeathFxFired 가 게이트)
else if (GetOwner())
	GetOwner()->SetActive(false);        // 즉시 (기본/현행)
}
```

### DeathFx Playable에 등록하기.

2. using DeathFx = std::function<void(const vmath::vec3&)>;

### 추가 확장해야 하는것 바로 WaveController에서 생성 파괴를 Observer Register, Unregister 를 수행해야함.
* 그렇게 해야 죽는 타이밍에 정확히 얼마나 많은 몬스터가 죽었는가를 얻을 수 있다.

---

### Audio

@apps/_MyApp_/src/Audio/AudioSystem.cpp 
거리에 따라, 플레이어 위치에 따라 BGM이 커지고 작아지면 안된다.

### WaveController의 StateMachine과 State에 의존하여

```
bool  mBgmPaused   = false;
int   mBgmStateIdx = 0;     // 0=Title, 1=Combat, 2=Boss
float mHealth      = 1.0f;  // global parameter
float mBgmVolume   = 1.0f;
float mSfxVolume   = 1.0f;
```
#### State에 따른.

```cpp
// 2) BGM_STATE — event-instance labeled (Title/Combat/Boss)
static const char *kBgmStateLabels[] = {"Title", "Combat", "Boss"};
if (ImGui::Combo("BGM_STATE", &mBgmStateIdx, kBgmStateLabels, 3))
{
	ck(mBgmInstance->setParameterByName("BGM_STATE",
	                                    static_cast<float>(mBgmStateIdx)),
	                                    "setParameterByName BGM_STATE");
}
```

#### Health에 따른

```
// 3) Health — global parameter (System level, 모든 이벤트 공유)
ImGui::Text("Global Parameter");
if (ImGui::SliderFloat("Health", &mHealth, 0.0f, 1.0f, "%.2f"))
{
	ck(mSystem->setParameterByName("Health", mHealth),
	                                    "setParameterByName Health");
}
```

로직 레퍼런싱

---

#### State 심화

##### Transition Graph

```
Title -> CombatPlay
CombatPlay -> Pause
CombatPlay -> GameOver
GameOver -> <NONE> 영원히 상태 유지
```

##### 현 프로젝트의 Time Logic State가 Wrapping 하도록
* Combat 상태에서 진행되야 하는것이고.
* Play, Pause, GameOver에서는 Global Time을 0으로 해야한다.
* @apps/_MyApp_/main.cpp#255-314의 render의 로직은 모두 State의 `virtual void OnUpdate(TOwner &owner, float dt) = 0;` 의탁되어야한다.

* State 별 Time
  1. Title : Global Time 0
  2. CombatPlay : 게임 시작 몬스터 생성 <-> Puase Global Time 적용
  3. Pause : Global Time 0
  4. GameOver : Global Time 0


##### State별 ImGUI 연출 및 화면 클릭
1. Title : Global Time 0
    PostFx Blur 적용
    Blur 위에 Override되는
    @apps/_MyApp_/resources/texture/Title.png
2. CombatPlay : 현 프로젝트의 로직 상황과 일치.
3. Pause
   PostFx Blur 적용
    Blur 위에 Override되는
    @apps/_MyApp_/resources/texture/Pause.png
4. GameOver 
   PostFx Blur 적용
   Blur 위에 Override되는
   @apps/_MyApp_/resources/texture/GameOver.png

#### 피격 범위 심화

#### 레이저 궁극기.

#### 총알 개선 방향으로 발사되야함

* 히트스캔 사용
/Users/escatrgot/DevelopProjects/SSU/GlobalMedia-OpenGL-ComputerGraphics/apps/_MyApp_/src/Physics/PhysicsRaycast.cpp

/Users/escatrgot/DevelopProjects/SSU/GlobalMedia-OpenGL-ComputerGraphics/apps/_MyApp_/src/Physics/PhysicsRaycast.h

### 이펙트
* Orbit 월드 중심에 회전 시키기
* 내가 음직인 자리에 더스트 생성하기.
* 피격될때 Hit 발생하기
* 플레이어 총알 발사시 올바른 방향으로 Effekseer 생성

### 1 2 3 4 5 6 7 8 9 로 패스 활성화 켜기 끄기.

### 플레이어 관절 Animation

---

```
최우선
### 이펙트 : 
    Orbit 월드 중심에 회전 시키기 
    내가 음직인 자리에 더스트 생성하기. 
    피격될때 Hit 발생하기 플레이어 총알 발사시 
    올바른 방향으로 Effekseer 생성

### 오디오 적용 : 
    총알 발사
    상시 들리는 BGM

중간 우선순위
### WaveController 분리
    State Machine으로 씬 활성화 

최하단 궁극기
### 이건 마지막날에 하자.
```