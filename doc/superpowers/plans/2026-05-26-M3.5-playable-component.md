# M3.5 — IPlayable Component 모듈 도입 Implementation Plan

> ⚠ 2026-05 시점 문서 (archival) — 코드 경로는 작성 당시 기준.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** spec `2026-05-26-playable-component-interface-design.md` 의 IPlayable + PlayableBase + Composite (Sequence/Parallel) + SpriteSequencePlayable 를 `src/playable/` + `src/sprite_sequence/` 코어 모듈로 추가하고, `_MyApp_` 의 SpriteAnimator 직접 호출을 SpriteSequencePlayable Component 부착으로 대체.

**Architecture:** 2-tier — `IPlayable` (pure interface, 저장소 `I*` 컨벤션) + `PlayableBase : IPlayable, SJH::Scene::Component` (다중 상속 abstract base, paused_/finished_/elapsed_/isLoop_ 보유). Composite (`SequencePlayable` / `ParallelPlayable`) 는 `std::vector<std::unique_ptr<IPlayable>>` + Tweeny/DOTween 정통 fluent Builder (`Append` / `Insert` / `Join` 가 derived 타입 참조 반환). Component 시스템의 `Update(float dt)` 가 Tick 역할 — 별도 PlayableTickSystem 미도입.

**Tech Stack:** C++17 / CMake STATIC 라이브러리 (본 저장소 `SJH::<module>` ALIAS 컨벤션) / SJH::Scene::Component 다중 상속 / vtable anchor 패턴 (.cpp 분리) / Catch2 v3 (옵션 — [[no_auto_tests]] 정책상 사용자 요청 시).

**검증 정책:** TDD red-green 미사용 ([[no_auto_tests]]). 각 Phase 끝에 *컴파일 통과 + 사용자 시각 확인* 게이트. 단위 테스트는 별도 요청 시.

---

## Phase 분해 (확정)

| Phase | 범위 | 의존 | 예상 commit | 게이트 |
|---|---|---|---|---|
| **A** | `src/playable/` 코어 (iplayable.h + playable_base + composite_playable) | SJH::Scene | 3 | `cmake --build` 통과 |
| **B** | `src/sprite_sequence/` (sprite_frame_clip.h + sprite_sequence_playable.h) | A + SJH::sprite | 2 | 동일 |
| **C** | `src/CMakeLists.txt` 우산 합류 + smoke 빌드 (`_MyApp_` / `migrate_demo` / `audio_demo` 통과) | A + B | 1 | 3종 데모 그린 |
| **D** | `_MyApp_` 마이그레이션 — SpriteAnimator 호출 제거 + PlayerActor 가 SpriteSequencePlayable Component 부착 | C | 2~3 | 사용자 시각 확인 |
| **E** | 문서 — CLAUDE.md (14→16 + M3.5 완료) + progress.md (~25% → 100%) + memory `next_work_playable` 갱신 | D | 1 | (게이트 없음) |

---

## Phase A — `src/playable/` 코어 모듈

### Task A.1 — 디렉토리 + IPlayable + PlayableBase + CMakeLists.txt

**Files:**
- Create: `src/playable/CMakeLists.txt`
- Create: `src/playable/iplayable.h`
- Create: `src/playable/playable_base.h`
- Create: `src/playable/playable_base.cpp` (vtable anchor)

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p src/playable
```

- [ ] **Step 2: `src/playable/iplayable.h` 작성** (spec §3.1 그대로)

```cpp
#ifndef __SJH_PLAYABLE_IPLAYABLE_H__
#define __SJH_PLAYABLE_IPLAYABLE_H__

namespace SJH::Playable
{
    /// @brief 시간축 추상화 — Client 우선 4 메서드 (Play/Pause/Stop/GetIsLoop) + 2급 IsFinished.
    /// @details
    ///   - 저장소 `I*` 컨벤션 준수: pure interface, 모든 멤버 =0, protected ctor + delete copy/move.
    ///   - 실제 상태/디폴트 임플리먼테이션은 `PlayableBase` (다중 상속 abstract base) 가 흡수.
    ///   - Tick 메서드 없음 — Component 시스템의 `Update(float dt)` 가 그 역할 (PlayableBase 흡수).
    class IPlayable
    {
      protected:
        IPlayable() = default;

      public:
        virtual ~IPlayable() = default;
        IPlayable(const IPlayable&)            = delete;
        IPlayable& operator=(const IPlayable&) = delete;
        IPlayable(IPlayable&&)                 = delete;
        IPlayable& operator=(IPlayable&&)      = delete;

        // === Client 우선 4-method ===
        virtual void Play()  = 0;       // 재생 시작 / Pause 후 재개 / Stop 후 첫 프레임부터
        virtual void Pause() = 0;       // 일시정지 (상태 보존, Play 로 재개)
        virtual void Stop()  = 0;       // 리셋 후 정지 (재사용 대기, 다음 Play 는 첫 프레임)
        virtual bool GetIsLoop() const = 0;

        // === 2급 공개 — Composite 의 child 종료 감지 + 외부 despawn 결정 ===
        virtual bool IsFinished() const = 0;
    };
}

#endif // __SJH_PLAYABLE_IPLAYABLE_H__
```

- [ ] **Step 3: `src/playable/playable_base.h` 작성** (spec §3.2 + dtor out-of-line 분리)

```cpp
#ifndef __SJH_PLAYABLE_PLAYABLE_BASE_H__
#define __SJH_PLAYABLE_PLAYABLE_BASE_H__

#include "playable/iplayable.h"
#include "scene/actor.h"   // SJH::Scene::Component

namespace SJH::Playable
{
    /// @brief IPlayable + Component 다중 상속 abstract base.
    ///        공통 상태 (paused_/finished_/elapsed_/isLoop_) + Play/Pause/Stop trivial 구현 + Update→OnUpdate hook.
    /// @note  concrete 는 OnUpdate 만 필수 override. OnPlay / OnStop 은 default empty.
    class PlayableBase : public IPlayable, public SJH::Scene::Component
    {
      protected:
        PlayableBase() = default;

      public:
        ~PlayableBase() override;   // out-of-line — vtable anchor (.cpp 에 정의)

        // === IPlayable 4-method + IsFinished ===
        void Play()  override
        {
            paused_   = false;
            finished_ = false;
            OnPlay();
        }
        void Pause() override { paused_ = true; }   // OnPause hook 없음 — concrete 가 Pause 자체 override 권장
        void Stop()  override
        {
            paused_   = false;
            finished_ = false;
            elapsed_  = 0.0f;
            OnStop();
        }
        bool GetIsLoop()  const override { return isLoop_; }
        bool IsFinished() const override { return finished_; }

        // === Loop setter — 인터페이스 외 추가 (사용자 결정 5-부속) ===
        void SetIsLoop(bool v) { isLoop_ = v; }

        // === Component 3 hook ===
        void OnEnter() override {}
        void OnExit()  override {}
        void Update(float dt) final
        {
            if (!IsEnabled() || paused_ || finished_) return;
            elapsed_ += dt;
            OnUpdate(dt);
        }

      protected:
        // === 파생 hook — concrete 가 override ===
        virtual void OnPlay()   {}
        virtual void OnStop()   {}
        virtual void OnUpdate(float dt) = 0;   // 유일 필수

        // === 파생 공유 상태 (protected) ===
        bool  paused_   = false;
        bool  finished_ = false;
        bool  isLoop_   = false;
        float elapsed_  = 0.0f;
    };
}

#endif // __SJH_PLAYABLE_PLAYABLE_BASE_H__
```

- [ ] **Step 4: `src/playable/playable_base.cpp` 작성** (vtable anchor)

```cpp
#include "playable/playable_base.h"

namespace SJH::Playable
{
    // out-of-line dtor — 다중 상속 vtable anchor.
    // 이 단일 컴파일 단위에 vtable 이 emit 되어 ODR 안정.
    PlayableBase::~PlayableBase() = default;
}
```

- [ ] **Step 5: `src/playable/CMakeLists.txt` 작성** (SJH::sprite 패턴 정통)

```cmake
# SJH::playable — IPlayable + PlayableBase + Composite (Sequence/Parallel)
# spec doc/superpowers/specs/2026-05-26-playable-component-interface-design.md
add_library(sjh_playable STATIC
    playable_base.cpp
    # composite_playable.cpp 는 Task A.2 에서 추가
)
add_library(SJH::playable ALIAS sjh_playable)

target_include_directories(sjh_playable PUBLIC
    ${CMAKE_SOURCE_DIR}/src
)

# PUBLIC — 헤더 노출 (IPlayable 은 헤더 only, PlayableBase 가 Component 헤더 노출)
target_link_libraries(sjh_playable PUBLIC
    SJH::Scene                # SJH::Scene::Component 베이스 (playable_base.h)
)
```

- [ ] **Step 6: 빌드 검증** — `src/CMakeLists.txt` 합류 전에 단독 컴파일 통과 확인 위해 임시 add_subdirectory

먼저 `src/CMakeLists.txt` 의 `add_subdirectory(sprite)` 다음 줄에 `add_subdirectory(playable)` 만 추가 (우산 합류는 Phase C 에서):

```cmake
add_subdirectory(sprite)
add_subdirectory(playable)   # ← M3.5 Task A.1
```

빌드:
```bash
cmake --preset ninja
cmake --build --preset ninja --target sjh_playable
```

Expected: `sjh_playable` STATIC 라이브러리 생성. 어떤 데모도 아직 link 안 하므로 warning/error 없어야 함.

- [ ] **Step 7: commit**

```bash
git add src/playable/ src/CMakeLists.txt
git commit -m "$(cat <<'EOF'
feat(playable): SJH::playable 모듈 신설 — IPlayable + PlayableBase

spec 2026-05-26 §3.1/§3.2 — pure interface + 다중 상속 abstract base.
- IPlayable: Play/Pause/Stop/GetIsLoop/IsFinished pure interface (저장소 I* 컨벤션)
- PlayableBase: IPlayable + SJH::Scene::Component 다중 상속, paused_/finished_/elapsed_/isLoop_ 흡수
- vtable anchor: ~PlayableBase() out-of-line (.cpp)

Composite (Sequence/Parallel) 는 Task A.2 에서 추가.

)"
```

---

### Task A.2 — Composite (SequencePlayable + ParallelPlayable + Builder)

**Files:**
- Create: `src/playable/composite_playable.h`
- Create: `src/playable/composite_playable.cpp`
- Modify: `src/playable/CMakeLists.txt` (sources 추가)

- [ ] **Step 1: `src/playable/composite_playable.h` 작성**

```cpp
#ifndef __SJH_PLAYABLE_COMPOSITE_PLAYABLE_H__
#define __SJH_PLAYABLE_COMPOSITE_PLAYABLE_H__

#include "playable/playable_base.h"
#include <memory>
#include <vector>

namespace SJH::Playable
{
    /// @brief 순차 — children 한 번에 1개. 현재 child 가 IsFinished 면 다음 child Play.
    ///        Loop=true 면 모든 children 종료 시 cursor=0 + Play() 재시작.
    /// @note  Builder Append/Insert 가 `SequencePlayable&` 반환 (derived 타입) — 체이닝 시 derived 메서드 유지.
    class SequencePlayable : public PlayableBase
    {
      public:
        // === Fluent Builder (Tweeny `*this` + DOTween Append/Insert 정통) ===
        SequencePlayable& Append(std::unique_ptr<IPlayable> child);
        SequencePlayable& Insert(std::size_t pos, std::unique_ptr<IPlayable> child);

        // 디버그/관찰
        std::size_t Size()   const { return children_.size(); }
        std::size_t Cursor() const { return cursor_; }

      protected:
        void OnPlay()   override;
        void OnStop()   override;
        void OnUpdate(float dt) override;

      private:
        std::vector<std::unique_ptr<IPlayable>> children_;
        std::size_t cursor_ = 0;
    };

    /// @brief 병렬 — children 모두 동시 Play. 모두 IsFinished 일 때 자기 IsFinished.
    ///        Loop=true 면 모든 children 종료 시 자동 Play 재시작 (무한 반복).
    class ParallelPlayable : public PlayableBase
    {
      public:
        ParallelPlayable& Join(std::unique_ptr<IPlayable> child);

        std::size_t Size() const { return children_.size(); }

      protected:
        void OnPlay()   override;
        void OnStop()   override;
        void OnUpdate(float dt) override;

      private:
        std::vector<std::unique_ptr<IPlayable>> children_;
    };
}

#endif // __SJH_PLAYABLE_COMPOSITE_PLAYABLE_H__
```

- [ ] **Step 2: `src/playable/composite_playable.cpp` 작성** (spec §3.3 — empty 가드 + dynamic_cast 디스패치 포함)

```cpp
#include "playable/composite_playable.h"

namespace SJH::Playable
{
    // ───── SequencePlayable ─────

    SequencePlayable& SequencePlayable::Append(std::unique_ptr<IPlayable> child)
    {
        children_.push_back(std::move(child));
        return *this;
    }

    SequencePlayable& SequencePlayable::Insert(std::size_t pos, std::unique_ptr<IPlayable> child)
    {
        if (pos > children_.size()) pos = children_.size();
        children_.insert(children_.begin() + pos, std::move(child));
        return *this;
    }

    void SequencePlayable::OnPlay()
    {
        cursor_ = 0;
        if (!children_.empty()) children_[0]->Play();
    }

    void SequencePlayable::OnStop()
    {
        for (auto& c : children_) c->Stop();
        cursor_ = 0;
    }

    void SequencePlayable::OnUpdate(float dt)
    {
        // 빈 컨테이너 가드 — isLoop_=true 라도 무한 빈 루프 회피
        if (children_.empty()) { finished_ = true; return; }

        if (cursor_ >= children_.size())
        {
            if (isLoop_)
            {
                cursor_ = 0;
                children_[0]->Play();
            }
            else
            {
                finished_ = true;
            }
            return;
        }

        // children 은 Component 이지만 Actor 미부착 — Composite 가 명시 Update 디스패치.
        // IPlayable* 보유라 PlayableBase* 로 down-cast 필요 (spec §3.3 trade-off).
        if (auto* head = dynamic_cast<SJH::Scene::Component*>(children_[cursor_].get()))
            if (head->IsEnabled()) head->Update(dt);

        if (children_[cursor_]->IsFinished())
        {
            ++cursor_;
            if (cursor_ < children_.size()) children_[cursor_]->Play();
        }
    }

    // ───── ParallelPlayable ─────

    ParallelPlayable& ParallelPlayable::Join(std::unique_ptr<IPlayable> child)
    {
        children_.push_back(std::move(child));
        return *this;
    }

    void ParallelPlayable::OnPlay()
    {
        for (auto& c : children_) c->Play();
    }

    void ParallelPlayable::OnStop()
    {
        for (auto& c : children_) c->Stop();
    }

    void ParallelPlayable::OnUpdate(float dt)
    {
        // 빈 컨테이너 가드 — vacuously true 로 finished_ 잘못 신호 회피
        if (children_.empty()) { finished_ = true; return; }

        bool allDone = true;
        for (auto& c : children_)
        {
            if (!c->IsFinished())
            {
                if (auto* head = dynamic_cast<SJH::Scene::Component*>(c.get()))
                    if (head->IsEnabled()) head->Update(dt);
                if (!c->IsFinished()) allDone = false;
            }
        }
        if (allDone)
        {
            if (isLoop_)
            {
                for (auto& c : children_) c->Play();
            }
            else
            {
                finished_ = true;
            }
        }
    }
}
```

- [ ] **Step 3: `src/playable/CMakeLists.txt` 의 sources 에 composite_playable.cpp 추가**

```cmake
add_library(sjh_playable STATIC
    playable_base.cpp
    composite_playable.cpp        # ← M3.5 Task A.2
)
```

- [ ] **Step 4: 빌드 검증**

```bash
cmake --build --preset ninja --target sjh_playable
```

Expected: `sjh_playable.a` 갱신, warning 없음. dynamic_cast 사용 코드라 `-Wno-error=pessimizing-move` 같은 기존 옵션이 영향 안 줘야 함.

- [ ] **Step 5: commit**

```bash
git add src/playable/composite_playable.h src/playable/composite_playable.cpp src/playable/CMakeLists.txt
git commit -m "$(cat <<'EOF'
feat(playable): SequencePlayable + ParallelPlayable + fluent Builder

spec 2026-05-26 §3.3 + §4-bis — Tweeny *this + DOTween Append/Insert/Join 정통.
- vector<unique_ptr<IPlayable>> 컨테이너 (concrete 만)
- Builder derived 타입 반환 (체이닝 + 다형 중첩)
- 빈 컨테이너 가드 (loop 무한 빈 루프 회피)
- dynamic_cast 디스패치 (children 이 IPlayable*, Component::Update 호출)

)"
```

---

### Task A.3 — Phase A 게이트 (단독 빌드 통과 확인)

**Files:** (변경 없음 — 검증만)

- [ ] **Step 1: clean + 전체 src 빌드**

```bash
rm -rf build_ninja
cmake --preset ninja
cmake --build --preset ninja --target sjh_playable
```

Expected: warning 0, error 0, `build_ninja/src/playable/libsjh_playable.a` 생성.

- [ ] **Step 2: include path 검증** — 다른 모듈에서 `#include "playable/iplayable.h"` 가능한지 syntax-only

임시로 `src/diagnostics/` 의 한 .cpp 에 `#include "playable/iplayable.h"` 한 줄 추가 → 빌드 → 통과 확인 후 라인 revert. (또는 skip — Phase B/C 에서 자연 검증)

- [ ] **Step 3: Phase A 완료 표시 (todo 갱신, commit 없음)**

---

## Phase B — `src/sprite_sequence/` 모듈

### Task B.1 — 디렉토리 + sprite_frame_clip + sprite_sequence_playable + CMakeLists.txt

**Files:**
- Create: `src/sprite_sequence/CMakeLists.txt`
- Create: `src/sprite/sprite_frame_clip.h`
- Create: `src/sprite/sprite_sequence_playable.h`
- Create: `src/sprite/sprite_sequence_playable.cpp` (OnUpdate out-of-line + vtable anchor)
- Modify: `src/CMakeLists.txt` (add_subdirectory)

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p src/sprite_sequence
```

- [ ] **Step 2: `src/sprite/sprite_frame_clip.h` 작성** (spec §3.4 POD)

```cpp
#ifndef __SJH_SPRITE_SEQUENCE_SPRITE_FRAME_CLIP_H__
#define __SJH_SPRITE_SEQUENCE_SPRITE_FRAME_CLIP_H__

namespace SJH::SpriteSequence
{
    /// @brief atlas frame index 시퀀스 정의 POD.
    ///        loop 필드 없음 — PlayableBase.isLoop_ 가 흡수 (spec §1 결정 2, §5 폐기 항목).
    struct SpriteFrameClip
    {
        int   startFrame;        // atlas 의 시작 frame index
        int   frameCount;        // 이 clip 의 frame 수 (1 이상)
        float fps;               // 초당 frame (0 보다 큰 양수)
    };
}

#endif // __SJH_SPRITE_SEQUENCE_SPRITE_FRAME_CLIP_H__
```

- [ ] **Step 3: `src/sprite/sprite_sequence_playable.h` 작성** (spec §3.4 — 선언만, 정의는 .cpp)

```cpp
#ifndef __SJH_SPRITE_SEQUENCE_SPRITE_SEQUENCE_PLAYABLE_H__
#define __SJH_SPRITE_SEQUENCE_SPRITE_SEQUENCE_PLAYABLE_H__

#include "playable/playable_base.h"
#include "src/sprite/sprite_frame_clip.h"
#include "sprite/sprite_component.h"   // SJH::Sprite::SpriteRenderer (frameIdx 갱신 대상)

namespace SJH::SpriteSequence
{
    /// @brief Sprite atlas frame 시퀀스 — PlayableBase.elapsed_ 흡수, OnUpdate 가 frame index 계산.
    /// @note  binding 주입: SpriteRenderer* + SpriteFrameClip*. 둘 다 외부 owner (Playable 은 raw ptr 만).
    class SpriteSequencePlayable : public SJH::Playable::PlayableBase
    {
      public:
        SpriteSequencePlayable(SJH::Sprite::SpriteRenderer* spriteRef,
                                const SpriteFrameClip*       clip);
        ~SpriteSequencePlayable() override;   // out-of-line vtable anchor

      protected:
        void OnUpdate(float dt) override;

      private:
        SJH::Sprite::SpriteRenderer* sprite_;
        const SpriteFrameClip*        clip_;
    };
}

#endif // __SJH_SPRITE_SEQUENCE_SPRITE_SEQUENCE_PLAYABLE_H__
```

- [ ] **Step 4: `src/sprite/sprite_sequence_playable.cpp` 작성** (OnUpdate + anchor)

```cpp
#include "src/sprite/sprite_sequence_playable.h"

namespace SJH::SpriteSequence
{
    SpriteSequencePlayable::SpriteSequencePlayable(SJH::Sprite::SpriteRenderer* spriteRef,
                                                    const SpriteFrameClip*       clip)
        : sprite_(spriteRef), clip_(clip)
    {
    }

    SpriteSequencePlayable::~SpriteSequencePlayable() = default;   // vtable anchor

    void SpriteSequencePlayable::OnUpdate(float /*dt*/)
    {
        if (!clip_ || !sprite_) return;
        // elapsed_ 는 PlayableBase 가 이미 += dt 처리 (Update final 안에서)
        if (clip_->fps <= 0.0f || clip_->frameCount <= 0) return;   // 잘못된 clip 가드

        const float frameDur = 1.0f / clip_->fps;
        int raw = static_cast<int>(elapsed_ / frameDur);

        if (isLoop_)
        {
            raw %= clip_->frameCount;                 // Phase 2 결정 — loop=true 자동 wrap
        }
        else if (raw >= clip_->frameCount)
        {
            raw = clip_->frameCount - 1;
            finished_ = true;                          // non-loop 자연 종료
        }
        sprite_->frameIdx = clip_->startFrame + raw;
    }
}
```

- [ ] **Step 5: `src/sprite_sequence/CMakeLists.txt` 작성** (SJH::sprite 패턴)

```cmake
# SJH::sprite_sequence — SpriteFrameClip + SpriteSequencePlayable
# spec doc/superpowers/specs/2026-05-26-playable-component-interface-design.md §3.4
add_library(sjh_sprite_sequence STATIC
    sprite_sequence_playable.cpp
)
add_library(SJH::sprite_sequence ALIAS sjh_sprite_sequence)

target_include_directories(sjh_sprite_sequence PUBLIC
    ${CMAKE_SOURCE_DIR}/src
)

# PUBLIC — 헤더 노출
target_link_libraries(sjh_sprite_sequence PUBLIC
    SJH::playable            # PlayableBase 베이스 (sprite_sequence_playable.h)
    SJH::sprite              # SpriteRenderer (sprite_sequence_playable.h)
)
```

- [ ] **Step 6: `src/CMakeLists.txt` 에 `add_subdirectory(sprite_sequence)` 추가**

`src/CMakeLists.txt` 의 `add_subdirectory(playable)` (Phase A.1 에서 추가) 다음 줄:

```cmake
add_subdirectory(playable)
add_subdirectory(sprite_sequence)   # ← M3.5 Task B.1
```

(우산 합류는 Phase C 에서 — 여기까지는 add_subdirectory 만)

- [ ] **Step 7: 빌드 검증**

```bash
cmake --build --preset ninja --target sjh_sprite_sequence
```

Expected: `sjh_sprite_sequence.a` 생성. SJH::playable / SJH::sprite 의존이 자동 해소되어 warning 0, error 0.

- [ ] **Step 8: commit**

```bash
git add src/sprite_sequence/ src/CMakeLists.txt
git commit -m "$(cat <<'EOF'
feat(sprite_sequence): SJH::sprite_sequence 모듈 신설 — SpriteFrameClip + SpriteSequencePlayable

spec 2026-05-26 §3.4 — SJH::playable + SJH::sprite 양쪽 의존 분리 모듈.
- SpriteFrameClip POD (startFrame/frameCount/fps) — loop 필드 폐기 (PlayableBase.isLoop_ 흡수)
- SpriteSequencePlayable : PlayableBase, OnUpdate 가 frame index 계산 + wrap/clamp
- OnUpdate out-of-line (.cpp) — vtable anchor 겸용
- 잘못된 clip (fps<=0, frameCount<=0) 가드 추가

)"
```

---

### Task B.2 — Phase B 게이트 (단독 빌드 통과 확인)

**Files:** (변경 없음 — 검증만)

- [ ] **Step 1: clean + 두 모듈 빌드**

```bash
rm -rf build_ninja
cmake --preset ninja
cmake --build --preset ninja --target sjh_playable
cmake --build --preset ninja --target sjh_sprite_sequence
```

Expected: 두 `.a` 모두 생성, warning 0, error 0.

- [ ] **Step 2: 헤더 의존 그래프 검증** (선택)

```bash
grep -rn "#include \"playable/" src/sprite_sequence/
grep -rn "#include \"sprite/" src/sprite_sequence/
```

Expected: sprite_sequence 가 playable + sprite 헤더만 참조 (다른 모듈 누설 없음).

- [ ] **Step 3: Phase B 완료 표시 (todo 갱신, commit 없음)**

---

## Phase C — `SJH::engine` 우산 합류 + smoke 빌드

### Task C.1 — `src/CMakeLists.txt` 우산 합류 (14 → 16 모듈)

**Files:**
- Modify: `src/CMakeLists.txt` (line 19-34 근방 — `target_link_libraries(sjhopengl_engine INTERFACE ...)` 라인 확장)

- [ ] **Step 1: `src/CMakeLists.txt` 갱신** — `sjhopengl_engine` INTERFACE link 에 두 모듈 추가

기존 (Phase A.1 + B.1 이후 상태):
```cmake
add_subdirectory(buffer)
add_subdirectory(object)
add_subdirectory(common)
add_subdirectory(diagnostics)
add_subdirectory(layout)
add_subdirectory(program)
add_subdirectory(material)
add_subdirectory(scene)
add_subdirectory(sprite)
add_subdirectory(fsm)
add_subdirectory(shader)
add_subdirectory(render)
add_subdirectory(resource_registry)
add_subdirectory(input)
add_subdirectory(playable)            # ← Phase A.1 에서 이미 추가
add_subdirectory(sprite_sequence)     # ← Phase B.1 에서 이미 추가

add_library(sjhopengl_engine INTERFACE)
add_library(SJH::engine ALIAS sjhopengl_engine)

target_link_libraries(sjhopengl_engine INTERFACE
    SJH::common
    SJH::buffer
    SJH::diagnostics
    SJH::input
    SJH::layout
    SJH::material
    SJH::object
    SJH::program
    SJH::render
    SJH::resource_registry
    SJH::Scene
    SJH::shader
    SJH::sprite          # M1 — 13 모듈 (sprite 합류)
    SJH::fsm             # M2 — 14 모듈 (fsm 합류)
)
```

갱신 (마지막 2 라인 추가):
```cmake
target_link_libraries(sjhopengl_engine INTERFACE
    SJH::common
    SJH::buffer
    SJH::diagnostics
    SJH::input
    SJH::layout
    SJH::material
    SJH::object
    SJH::program
    SJH::render
    SJH::resource_registry
    SJH::Scene
    SJH::shader
    SJH::sprite             # M1 — 13 모듈 (sprite 합류)
    SJH::fsm                # M2 — 14 모듈 (fsm 합류)
    SJH::playable           # M3.5 — 15 모듈 (playable 합류)
    SJH::sprite_sequence    # M3.5 — 16 모듈 (sprite_sequence 합류)
)
```

- [ ] **Step 2: smoke 빌드 — 활성 데모 3종 (`_MyApp_` / `migrate_demo` / `audio_demo`)**

```bash
rm -rf build_ninja
cmake --preset ninja
cmake --build --preset ninja --target _MyApp_
cmake --build --preset ninja --target migrate_demo
cmake --build --preset ninja --target audio_demo
```

Expected:
- 3종 모두 link 성공 (`SJH::engine` 우산이 자동 propagation)
- warning 0 (M3.5 모듈 신규 추가가 기존 데모에 영향 없어야 함)
- 실제 데모는 SpriteSequencePlayable 아직 미사용 — Phase D 에서 도입

- [ ] **Step 3: 실행 smoke (선택)** — 빌드만 성공해도 게이트 통과지만 추가 안심:

```bash
cd build_ninja/apps/_MyApp_ && ./_MyApp_ &
sleep 2
killall _MyApp_ 2>/dev/null
cd -
```

Expected: 윈도우 뜨고 2초 후 정상 종료. crash/abort 없음. (시각 검증은 Phase D 에서 본격)

- [ ] **Step 4: commit**

```bash
git add src/CMakeLists.txt
git commit -m "$(cat <<'EOF'
build(engine): SJH::engine 우산에 playable + sprite_sequence 합류 (14 → 16 모듈)

spec 2026-05-26 §2.2 — INTERFACE 우산이 두 신규 모듈을 자동 propagation.
- SJH::playable      (M3.5 — 15 모듈)
- SJH::sprite_sequence (M3.5 — 16 모듈)

smoke 빌드 통과: _MyApp_ / migrate_demo / audio_demo 3종.
실제 사용 도입은 Phase D — _MyApp_ 의 SpriteAnimator → SpriteSequencePlayable 치환.

)"
```

---

### Task C.2 — Phase C 게이트 (smoke 그린 확인)

**Files:** (변경 없음)

- [ ] **Step 1: 3 데모 link 결과 grep**

```bash
nm build_ninja/apps/_MyApp_/_MyApp_ 2>/dev/null | grep -E "SJH::Playable::" | head -5
```

Expected (선택 검증): `_MyApp_` 가 아직 SJH::Playable 심볼 사용 안 하므로 결과 empty 가 정상. (Phase D 이후 결과가 생김)

- [ ] **Step 2: header 영향 grep**

```bash
grep -rn "playable/" apps/ 2>/dev/null
grep -rn "sprite_sequence/" apps/ 2>/dev/null
```

Expected: 모두 0건 (Phase D 이전이라 _MyApp_ 가 아직 include 안 함).

- [ ] **Step 3: Phase C 완료 표시 (todo 갱신, commit 없음)**

---

## Phase D — `_MyApp_` 마이그레이션 (보류 — 후속 plan 으로 분리)

> **상태**: 2026-05-26 사용자 결정으로 *본 plan 범위에서 제외*. 사전조사 결과 마이그레이션 깊이가 spec §6.1 가이드보다 큼 (Pull→Push 패턴 전환 + SpriteRenderer 도입 필요) — 별도 plan/세션에서 본격 결정 후 진행.

### 사전조사 결과 (보존 — 후속 작업자용)

**SpriteAnimator 사용처 4곳** (`apps/_MyApp_/main.cpp`):
- `:33` — `#include "<sprite>/sprite_animator.h"`
- `:180` — `mAnimator = spriteActor->AddComponent<SJH::Sprite::SpriteAnimator>();`
- `:181-182` — `SetFps(4.0f); SetAtlas(&mAtlas);`
- `:214-216` — `if (mAnimator && mAtlasMaterial) { SJH::Uniforms::SetVec4(*mAtlasMaterial, "uUvRect", mAtlas.GetUVRect(mAnimator->GetCurrentFrame())); }`
- `:273` — `SJH::Sprite::SpriteAnimator *mAnimator = nullptr;` (멤버 변수)

**현재 흐름 = Pull 패턴**:
1. PlayerActor 부착 → 자동 Update (frame 계산)
2. render() 가 매 프레임 `mAnimator->GetCurrentFrame()` 조회
3. `mAtlas.GetUVRect(frame)` → `SetVec4(uUvRect, ...)` 직접 송신

**SpriteSequencePlayable = Push 패턴** (spec §3.4):
- ctor: `SpriteSequencePlayable(SpriteRenderer* sprite, SpriteFrameClip* clip)`
- OnUpdate 가 `sprite_->frameIdx = startFrame + raw` 직접 갱신
- render() 는 sprite_->frameIdx 를 *어디선가* 소비해야 함

**구조적 발견**:
- `SpriteRenderer` 는 `src/sprite/sprite_component.h` 에 이미 존재 — 멤버: `atlas` + `frameIdx` + size/tint/flipX. Update 는 empty (Playable 갱신 가정)
- **`_MyApp_` 는 SpriteRenderer 미사용** (grep 결과 0건) — main.cpp 가 SpriteRenderer 없이 직접 mAtlas + uniform 송신
- `PlayerActor.h` 도 SpriteRenderer 부착 안 함 (Life/Movement/Physics/Controller 만)

### 후속 plan 결정 옵션 (보존)

| 옵션 | 범위 | commit | 영향 |
|---|---|---|---|
| **완전 마이그레이션 (spec §6.1 정통)** | PlayerActor 에 SpriteRenderer 추가 부착 + SpriteSequencePlayable 부착 + main.cpp render() 의 uUvRect 송신을 SpriteRenderer.frameIdx 기반으로 전환 + SpriteAnimator 완전 제거 | 5~6 | 넓음 (main.cpp + PlayerActor + 가능하면 Material 시스템 영향) |
| **최소 마이그레이션 (병행 전환)** | SpriteSequencePlayable 도입만, SpriteAnimator 와 *병행 부착* — 동작 동등 검증 후 별도 commit 으로 SpriteAnimator 제거 | 2 | 좁음 (점진적 전환) |
| **spec 변경 — SpriteRenderer 분리** | SpriteSequencePlayable ctor 를 `SpriteRenderer*` → `int* outFrameIdx` 로 변경. spec §3.4 수정 필요. PlayerActor 는 int 멤버만 추가 | 3 | spec 변경 비용 |

### 결정 게이트 (재진입 시)

이 plan 의 Phase A/B/C/E 완료 후 사용자 확인 받고 위 3 옵션 중 선택 → 별도 plan `<doc>/superpowers/plans/2026-05-XX-_MyApp_-spritesequence-migration.md` 신설.

---

## Phase E — 문서 갱신 (Phase D 보류 가정 — *부분 완료* 반영)

> **전제**: Phase D 보류로 SpriteAnimator → SpriteSequencePlayable 실제 호출처 치환 미진행. M3.5 의 *코어 모듈 도입* 단계만 완료. progress.md / memory 도 *부분 완료* 로 갱신.

### Task E.1 — `.claude/CLAUDE.md` 갱신 (모듈 14 → 16 + M3.5 부분 완료)

**Files:**
- Modify: `.claude/CLAUDE.md`

- [ ] **Step 1: Architecture §3 의 모듈 카운트 라인 갱신**

기존:
```
3. `src/` — 14개 코어 모듈 STATIC 라이브러리 (`SJH::<module>` ALIAS 컨벤션) — buffer, common, diagnostics, **fsm**, input, layout, material, object, program, render, resource_registry, scene, shader, **sprite**. 14개 모두를 묶은 **INTERFACE 우산 타겟 `SJH::engine`** ([src/CMakeLists.txt:16-34](src/CMakeLists.txt#L16-L34))
```

갱신:
```
3. `src/` — 16개 코어 모듈 STATIC 라이브러리 (`SJH::<module>` ALIAS 컨벤션) — buffer, common, diagnostics, **fsm**, input, layout, material, object, **playable**, program, render, resource_registry, scene, shader, **sprite**, **sprite_sequence**. 16개 모두를 묶은 **INTERFACE 우산 타겟 `SJH::engine`** ([src/CMakeLists.txt:16-36](src/CMakeLists.txt#L16-L36))
```

또한 마지막의 *차기 추가 예정 모듈* 문장 제거 (이미 도입 완료):
- 기존: "**차기 추가 예정 모듈 (spec §1.5/1.6)** — `SJH::playable` (Composite 패턴 Playable/Sequence/Parallel) + `SJH::sprite_sequence` (SpriteFrameClip + SpriteSequencePlayable) → 도입 시 16개."
- 갱신: "**M3.5 도입 완료 (2026-05-26)** — `SJH::playable` (IPlayable + PlayableBase + Composite) + `SJH::sprite_sequence` (SpriteFrameClip + SpriteSequencePlayable). spec [`doc/superpowers/specs/2026-05-26-playable-component-interface-design.md`]. _MyApp_ 의 SpriteAnimator → SpriteSequencePlayable 호출처 치환은 *보류* (후속 plan)."

- [ ] **Step 2: `_MyApp_` 설명의 *다음 작업* 갱신**

§"Active Target Management" 의 `_MyApp_` 행 안:

기존:
```
**다음 = M3.5 Playable 본격 도입** — spec §1.5/1.6 의 `SJH::playable` + `SJH::sprite_sequence` 별도 모듈화
```

갱신:
```
**M3.5 코어 모듈 도입 완료 (2026-05-26)** — `SJH::playable` + `SJH::sprite_sequence` 별도 모듈화 done. 다음 = _MyApp_ 마이그레이션 결정 (3 옵션 — 완전/최소/spec 변경) 또는 M4 본격 (PlayerStateMachine + Bullet + 적)
```

- [ ] **Step 3: 모듈 표에 `SJH::playable` + `SJH::sprite_sequence` 행 추가** (Architecture §"Src 모듈 레이아웃" 표)

`SJH::sprite` 행 다음에 두 행 추가:

```markdown
| `SJH::playable` *(M3.5 추가)* | `IPlayable` pure interface + `PlayableBase` abstract base (다중 상속 — IPlayable + Component, paused_/finished_/elapsed_/isLoop_ 흡수 + Play/Pause/Stop trivial + Update→OnUpdate hook) + `SequencePlayable` / `ParallelPlayable` Composite (vector<unique_ptr<IPlayable>> + Tweeny/DOTween 정통 fluent Builder `Append`/`Insert`/`Join`). spec [`doc/superpowers/specs/2026-05-26-playable-component-interface-design.md`] |
| `SJH::sprite_sequence` *(M3.5 추가)* | `SpriteFrameClip` POD (startFrame/frameCount/fps — loop 필드는 PlayableBase.isLoop_ 흡수) + `SpriteSequencePlayable : PlayableBase` (OnUpdate 가 `sprite_->frameIdx` 직접 갱신 — Push 패턴, loop=true 자동 wrap, non-loop 자연 종료). PUBLIC 의존: `SJH::playable` + `SJH::sprite` |
```

또한 `SJH::engine` 행의 "14개" → "16개"

---

### Task E.2 — `doc/topdown-shooter-progress.md` 갱신 (M3.5 ~25% → ~75%)

**Files:**
- Modify: `doc/topdown-shooter-progress.md`

- [ ] **Step 1: 진행 요약 표의 M3.5 행 갱신**

기존:
```
| **M3.5** Playable + sprite_sequence | 🟡 SpriteAnimator 경량 도입 | ~25% |
```

갱신:
```
| **M3.5** Playable + sprite_sequence | 🟢 코어 모듈 도입 완료 (마이그레이션 보류) | ~75% |
```

- [ ] **Step 2: M3.5 섹션 안에 *현재 상태* 갱신**

기존 M3.5 표:
```
| `SJH::playable` 모듈 (Playable / Sequence / Parallel / PlayerComponent / TickSystem) | ❌ 미시작 |
| `SJH::sprite_sequence` 모듈 (SpriteFrameClip + SpriteSequencePlayable) | ❌ 미시작 |
| `SpriteAnimator` (경량, src/sprite/ 내부) | ✅ `c4fd046` |
| Composite 트리 (Sequence + Parallel chaining) | ❌ — M5/M6 시점 |
| 단위 테스트 | ❌ |
```

갱신:
```
| `SJH::playable` 모듈 (IPlayable / PlayableBase / SequencePlayable / ParallelPlayable) | ✅ 2026-05-26 (3 commit — Phase A 결과) |
| `SJH::sprite_sequence` 모듈 (SpriteFrameClip + SpriteSequencePlayable) | ✅ 2026-05-26 (1 commit — Phase B 결과) |
| `SJH::engine` 우산 합류 (14 → 16 모듈) | ✅ 2026-05-26 (1 commit — Phase C 결과) |
| `SpriteAnimator` (경량, src/sprite/ 내부) | 🟡 미폐기 (Phase D 보류 — _MyApp_ 마이그레이션 미진행) |
| Composite 트리 (Sequence + Parallel chaining) | ✅ Composite 자체 도입. 실사용은 M5/M6 |
| _MyApp_ 의 SpriteAnimator → SpriteSequencePlayable 치환 | ❌ 보류 — 후속 plan |
| 단위 테스트 | ❌ ([[no_auto_tests]]) |
```

- [ ] **Step 3: 변경 기록 행 추가**

```
| 2026-05-26 | **M3.5 코어 모듈 도입 완료** — SJH::playable (IPlayable + PlayableBase + Composite) + SJH::sprite_sequence (SpriteFrameClip + SpriteSequencePlayable) 별도 모듈화 + SJH::engine 우산 합류 14→16. spec `2026-05-26-playable-component-interface-design.md`. _MyApp_ 의 SpriteAnimator 호출처 치환은 *보류* (Phase D — 3 옵션 결정 미정). 진행도 ~25% → ~75%. |
```

---

### Task E.3 — `<memory>/next_work_playable.md` 갱신 (다음 작업 방향 재설정)

**Files:**
- Modify: `/Users/escatrgot/.claude/projects/-Users-escatrgot-DevelopProjects-SSU-GlobalMedia-OpenGL-ComputerGraphics/memory/next_work_playable.md`

- [ ] **Step 1: memory body 전체 재작성 — *코어 도입 완료 + 다음 작업 결정* 으로 전환**

```markdown
---
name: next-work-playable
description: M3.5 코어 모듈 (SJH::playable + SJH::sprite_sequence) 도입 완료 (2026-05-26). 다음 작업 = SpriteAnimator 호출처 치환 *결정* 또는 M4 진입.
metadata:
  type: project
---

`_MyApp_` 탑다운 슈터의 **M3.5 코어 모듈 도입 완료** (2026-05-26):
- `SJH::playable` (IPlayable + PlayableBase + Composite Sequence/Parallel + Tweeny/DOTween 정통 fluent Builder)
- `SJH::sprite_sequence` (SpriteFrameClip + SpriteSequencePlayable)
- `SJH::engine` 우산 합류 (14 → 16 모듈)

**남은 작업 — Phase D 보류 결정 (2026-05-26)**: `_MyApp_` 의 SpriteAnimator 직접 호출을 SpriteSequencePlayable Component 부착으로 치환하는 작업이 *spec §6.1 가이드보다 큰 범위* 임을 사전조사로 확인 (Pull→Push 패턴 전환 + SpriteRenderer 도입 필요). 별도 plan 으로 분리.

**대상 spec (정본)**:
- `doc/superpowers/specs/2026-05-26-playable-component-interface-design.md` — M3.5 본 spec (7 결정 확정)
- `doc/superpowers/plans/2026-05-26-M3.5-playable-component.md` — 본 plan, Phase D 사전조사 결과 + 3 옵션 보존

**다음 작업 결정 — 3 옵션** (재진입 시 사용자 선택 필요):
1. **완전 마이그레이션** — PlayerActor 에 SpriteRenderer 추가 + SpriteSequencePlayable 부착 + main.cpp render() 의 uUvRect 송신 전환 + SpriteAnimator 완전 제거. 5~6 commit. spec §6.1 정통.
2. **최소 마이그레이션** — SpriteSequencePlayable 도입만, SpriteAnimator 와 *병행 부착* 으로 동작 동등 검증 후 별도 commit 에서 SpriteAnimator 제거. 2 commit. 점진적.
3. **spec 변경** — SpriteSequencePlayable ctor 를 `SpriteRenderer*` → `int* outFrameIdx` 로 변경. PlayerActor 는 int 멤버만 추가. 3 commit. spec §3.4 수정 비용.

**또는 M4 본격 진입**: PlayerStateMachine + Bullet + 적 — SpriteAnimator 잔존은 *기술 부채* 로 두고 M4 우선.

**호스트 컨벤션** (변경 없음 — spec §1 결정 4): 한 Actor 가 둘 다 부착 금지가 *컨벤션* 임 — Composite Playable + FSM 둘 다 부착은 spec 가 *재해석* 으로 허용 (같은 갱신원 갈리지 않는 한). [[fsm-state-machine-host]] 참조.

**How to apply**:
- "다음 작업 뭐?" / "M3.5 마무리" 질문 → 위 3 옵션 + M4 진입 중 선택 권유
- 마이그레이션 진행 시 plan `2026-05-26-M3.5-playable-component.md` 의 *Phase D 사전조사 결과* 섹션을 후속 plan 의 입력으로 사용 (재조사 불필요)
- M4 진입 시 SpriteAnimator 는 *기술 부채 카탈로그* 에 명시 — 사용자 시각 확인 필요해지면 언제든 마이그레이션 진행
```

---

### Task E.4 — 문서 3건 한 commit

**Files:** (이전 Task 의 modify 들)

- [ ] **Step 1: 변경 확인**

```bash
git status
git diff .claude/CLAUDE.md doc/topdown-shooter-progress.md
```

Expected: 3 파일 (CLAUDE.md / progress.md / memory next_work_playable.md). memory 는 별도 git repo 일 수도 있음 — 확인 후 가능하면 commit, 아니면 skip.

- [ ] **Step 2: commit**

```bash
git add .claude/CLAUDE.md doc/topdown-shooter-progress.md
git commit -m "$(cat <<'EOF'
docs(M3.5): 코어 모듈 도입 완료 반영 — CLAUDE.md 14→16 + progress.md ~75%

plan doc/superpowers/plans/2026-05-26-M3.5-playable-component.md Phase E.
- CLAUDE.md: 모듈 14→16, _MyApp_ "다음 작업" 갱신, 모듈 표에 playable/sprite_sequence 행 추가
- progress.md: M3.5 ~25% → ~75% (코어 도입 완료, 마이그레이션 보류), 변경 기록 행 추가

_MyApp_ 의 SpriteAnimator 호출처 치환은 *보류* — Phase D 사전조사 결과 spec §6.1 가이드보다
큰 마이그레이션 범위 (Pull→Push 패턴 + SpriteRenderer 도입 필요) 확인. 후속 plan 분리.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(memory `next_work_playable.md` 는 `~/.claude/projects/...` 경로 — 본 repo 외. 별도 처리 또는 skip)

---

## Self-Review (Phase A 만 — Phase B/D/E 는 상세화 시점에 재실시)

**Spec coverage (Phase A 한정)**:
- ✅ §3.1 IPlayable pure interface — Task A.1 Step 2
- ✅ §3.2 PlayableBase 다중 상속 — Task A.1 Step 3/4 (dtor anchor 추가)
- ✅ §3.3 SequencePlayable / ParallelPlayable + Builder — Task A.2
- ✅ §4 빈 컨테이너 가드 + dynamic_cast trade-off 명시 — composite_playable.cpp

**Placeholder scan (Phase A)**: 없음 (TBD/TODO 0). Phase B/C/D/E 는 "상세화 시점에 작성" 으로 의도된 deferred.

**Type consistency (Phase A)**:
- `PlayableBase::Update(float dt)` `final` — composite/leaf 가 OnUpdate hook override
- `dynamic_cast<SJH::Scene::Component*>` — IPlayable -> Component (다중 상속 cross-cast)
- `std::unique_ptr<IPlayable>` 컨테이너 일관

**알려진 trade-off**:
- composite_playable.cpp 의 dynamic_cast — 향후 IPlayable 의 비-PlayableBase 구현이 생기지 않는 한 사실상 always-success. 단점: 매 프레임 RTTI 비용. 대안 (children 을 `unique_ptr<PlayableBase>` 로 강타입) 은 spec §3.3 footnote 의 *유연성 우선* 결정에 따라 보류.
