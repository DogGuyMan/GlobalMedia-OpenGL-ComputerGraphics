// SJH::Effect 는 inline-only — dtor = default + Ref() inline.
// effect.cpp 는 add_library 의 소스 슬롯 유지를 위한 빈 TU.
#include "effect.h"

namespace SJH
{
    // (intentionally empty — Effect 는 inline-only)
}
