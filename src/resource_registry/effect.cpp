/**
 * @file effect.cpp
 * @brief @c Effect 구현 TU - 메서드가 모두 inline 이므로 본문 없음.
 *
 * @details
 *  @c Effect 는 inline-only (@c dtor = default, @c Ref() inline) - 별도 정의 불필요.
 *  본 파일은 @c CMakeLists.txt 의 @c add_library 소스 슬롯 유지용 빈 TU.
 */
#include "effect.h"

namespace SJH
{
    // (intentionally empty - Effect 는 inline-only)
} // namespace SJH
