/**
 * @file material.cpp
 * @brief Material 모듈 구현부 — 현재 헤더 inline 으로 완결, 별도 정의 없음.
 *
 * @details
 *  Apply() 는 SP3 T3 Pattern Y 결정으로 제거됨. GL 호출은 RenderContext(향후 MaterialApplier)
 *  게이트웨이가 전담. 본 TU 는 헤더만으로 완결되어 빈 상태로 유지.
 */
#include "material/material.h"
