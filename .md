[ROLE]
당신은 OpenGL Computer Graphics 프로젝트 (C++17 CMake) 의 렌더 모듈 리팩토링을 이어받는 Claude Agent 입니다. 이전 세션의 종합 메모리가 정리된 상태이며, 본 메시지는 *그 위에 이어지는* 작업의 진입 프롬프트입니다.

[FIRST READ — 반드시 순서대로]
1. `doc/design/2026-05-26-render-refactor-session.md` — 이전 세션 종합 메모리. commit chain (fc4ad0a~6c6c243), 채택 6종/거부 5종 결정, Context7 4 엔진 리서치, 미진행 SP 4종 명세. **본 메시지의 모든 결정은 이 문서의 §3 (거부) + §4 (4엔진) + §5.4 (SP-UniversalRT) 와 정합해야 함.**
2. `.claude/CLAUDE.md` — 프로젝트 컨벤션 + 자원 보유 컨벤션. 모든 자원은 ResourceRegistry 가 관리. 코어 모듈은 spdlog 미사용.
3. `.claude/skills/clean-ddd-hexagonal/SKILL.md` — 본 작업의 평가 기준. Dependency Rule / Ports & Adapters / LSP / Anti-pattern 8종.

[CURRENT STATE]
- 브랜치: `game/module/rendertarget`
- HEAD: `6c6c243 [dev] : material eager delete`
- 시각 회귀 대기: `migrate_demo` + `_MyApp_` (tweeny_demo 는 제외 — 사용자 결정)
- Material::SetProgram 본문은 `mProgram = program; return *this;` 만 (EagerBuild 제거 상태)

[TASK — SP-UniversalRenderTarget]
사용자 철학: **"모든 렌더링 대상은 RenderTarget 을 가진다"**. Camera 도 *반드시* 자기 RenderTarget 을 보유해야 한다. backbuffer 직접 그리기는 *최종 ScreenQuad pass 만* 의 권한.

4 엔진 정통 부합 (Unity URP RTHandle / Unreal FSceneRenderer / Godot Viewport / Cocos forward-add) — 본 작업은 *정통 합류*.

[PHASE 분해 — 순서 준수]

### Phase A. `ScreenQuadStage` 신설 (독립, 가장 먼저)
- `src/render/screen_quad_stage.{h,cpp}` 신설 — `IRenderStage` 구현
- 책임: N 개 입력 RenderTarget (FBO) 의 color attachment 를 단일 backbuffer pass 로 합성
- 입력: `std::vector<Framebuffer*>` 또는 `RenderTarget*` (color attachment 노출 인터페이스 필요)
- 셰이더: `passthrough.vs` + `composite.fs` 신설 (texture sampler N 개 — 또는 N=1 부터 시작 후 확장)
- 변경 0 — 다른 stage 가 본 stage 를 *옵트인* 으로 사용

### Phase B. Camera 강제 non-null + SceneRenderer fallback 제거
- `Camera::SetTargetRenderTarget(nullptr)` 호출 → `assert` (hidden astonishment 회피 — 본 컨벤션은 *명시적 강제*)
- `SceneRenderer::RenderWithCamera` 의 `defaultTarget` fallback **로직 삭제**:
  ```
  // 폐기:
  RenderTarget &target = cam.GetTargetRenderTarget() ? *cam.GetTargetRenderTarget() : defaultTarget;
  // 신규:
  assert(cam.GetTargetRenderTarget());
  RenderTarget &target = *cam.GetTargetRenderTarget();
  ```
- 데모 2개 (`migrate_demo` / `_MyApp_`) 의 각 카메라에 FBO 명시 할당 + ScreenQuadStage 등록
- `tweeny_demo` 는 본 SP 에서 *제외* (사용자 결정)

### Phase C. Overlay 컨셉 정리
- `Camera::Depth` 정렬은 *ScreenQuadStage 의 합성 순서* 로 격하 — Camera 가 직접 backbuffer 에 덮어 그리는 패턴 폐기
- Layer 시스템 (`Scene::Layer` enum class uint64_t) 은 유지 — culling mask 로 살아있음
- 데모의 Overlay 패턴 (있다면) 을 *FBO 카메라 → ScreenQuadStage 합성 순서* 로 변환

[수용 기준 — 모든 항목 만족해야 commit]
- ✅ `migrate_demo` 시각 동일 + 콘솔 warn 0 건
- ✅ `_MyApp_` 시각 동일 + 콘솔 warn 0 건
- ✅ `Camera::mTargetRT == nullptr` 으로 생성 후 SceneRenderer 진입 시 assert 발화
- ✅ `cmake --build --preset ninja --target migrate_demo _MyApp_` 빌드 성공
- ✅ 활성 단위 테스트 통과 (ctest — _NOT_BUILT 2개 외 모두 pass)
- ✅ tweeny_demo 는 *손대지 않음* (빌드 깨져도 본 SP 책임 외)

[금지 사항 — 이전 거부 결정 함정]
1. ❌ **DeviceContext 에 Uniforms 흡수 시도** — `glUniform*` 의 fallback 진단 가시성 가려짐. R3 docstring 의 분리 원칙 유지
2. ❌ **Material + Light 공통 `Apply` 인터페이스** — LSP 부재 + 4 엔진 정통 4 for 4 분리. 도메인 간 우연한 메서드 이름 결합 금지
3. ❌ **EagerBuild 재도입 (양방향 모두)** — 사용자가 prune/슬롯채움 양방향 모두 폐기. schema 검증은 future SP 의 *명시 schema 도입* 으로 분리
4. ❌ **DefaultRenderTarget 폐기** — 추상은 유지. *유일 소비자가 ScreenQuadStage* 가 되도록 책임 재정의만
5. ❌ **Camera::mTargetRT nullable 유지** — 본 SP 의 핵심 결정에 정면 위반
6. ❌ **tweeny_demo 마이그레이션** — 사용자 명시 제외. 빌드 deactivate 또는 skip
7. ❌ **자원의 데모 main 직접 보유** — `ProgramUPtr`/`FramebufferUPtr` 등 멤버화 금지. `ResourceRegistry` 위탁 (단, Program/Framebuffer 의 ResourceRegistry 미지원은 future `SP-ProgramRegistry` 영역 — 본 SP 에서는 *기존 위반 유지* 가 안전)

[컨벤션 가드레일]
- 한국어 주석 + 사용자 소통
- 헤더 가드 `__SJH_*_H__` (대문자 + 언더스코어, `#pragma once` 미사용)
- `.clang-format` (Microsoft, Tab=4, ColumnLimit=0)
- `long` 금지 → `int32_t`/`uint64_t` 등 고정 크기
- `windows.h` 는 `#ifdef _WIN32` 안에서만 + `WIN32_LEAN_AND_MEAN` + `NOMINMAX`
- ResourceRegistry 의 destroy 순서 = Material 먼저, Program 나중 (Observer 부재 시 lifetime 보장의 근거)

[체크인 시점 — 각 Phase 끝]
- Phase A 끝: ScreenQuadStage 단위 동작 확인 (단순 색 합성 데모 또는 단위 테스트)
- Phase B 끝: 2 데모 빌드 + 시각 회귀 사용자 확인 → commit
- Phase C 끝: Overlay 패턴 완전 제거 확인 + 시각 회귀 사용자 확인 → commit

[진행 모드]
사용자와 한국어로 소통. brainstorming 스킬은 사용자가 이미 결정한 사항이므로 *skip*. writing-plans 또는 refactor-pass 스킬 진입 가능. subagent-driven-development 권장 (각 Phase 별 fresh subagent + 사용자 시각 회귀 게이트).

[질문해야 할 시점]
- ScreenQuadStage 의 합성 모드 (단순 alpha blend / additive / replace) 가 데모별로 다르다면 *시작 전 사용자 확인*
- Layer enum 의 culling mask 와 ScreenQuadStage 합성 순서 간 일관성 점검 필요 시 *명시 질문*

위 컨텍스트 읽은 후 **첫 행동**: `doc/design/2026-05-26-render-refactor-session.md` 의 §5.4 SP-UniversalRenderTarget 를 정독하고, 본 메시지의 Phase A 진입 전 *현재 데모의 Camera→RT 사용 패턴 grep + 보고*. 그 다음 사용자 승인 받고 Phase A 착수.
