```mermaid
classDiagram
    direction LR

    namespace SJH {
        class Shader {
            -GLuint mShaderAddr
            +CreateFromFile()$ ShaderUPtr
            +CreateFromSource()$ ShaderUPtr
            +GetShaderAddr() GLuint
            +~Shader()
        }
        class Program {
            -GLuint mProgramAddr
            +Create(shaders)$ ProgramUPtr
            +CreateWithVSFS(vs, fs)$ ProgramUPtr
            +Use()
            +GetProgramAddr() GLuint
            +~Program()
        }
        class Uniforms {
            <<namespace · free functions>>
            +BuildCache(Program&)
            +Forget(GLuint)
            +SetMat4(prog, name, m)
            +SetVec3(prog, name, v)
            +SetFloat / SetInt / SetVec2 / SetVec4
            +SetDirLight / SetPointLight / SetSpotLight
        }
    }

    namespace EngineLegacy {
        class ShaderProgram {
            <<abstract>>
            +GLuint ProgAddr
            +ClearBuffer()
            +Apply(view, proj, viewPos)*
            +Render(models, view, proj, viewPos)
            +~ShaderProgram()
        }
        class DefaultShaderProgram {
            +Apply(view, proj, viewPos)
        }
        class TextureShaderProgram {
            +Light* AttachedLight
            +Apply(view, proj, viewPos)
        }
    }

    Program ..> Shader : ShaderPtr (attach)
    Uniforms ..> Program : 캐시 key = GLuint
    ShaderProgram <|-- DefaultShaderProgram
    ShaderProgram <|-- TextureShaderProgram

    note for ShaderProgram "LEGACY (src/engine/)\nsrc/CMakeLists.txt 에 engine 미등록 — dead code\n복사·이동 암묵 (= delete 없음)"
    note for Program "복사·이동 암묵\nProgram copy = *prog; 컴파일됨 → 이중 glDeleteProgram"
    note for Shader "복사·이동 암묵 (Program 과 동일 위험)"
```

---



```mermaid
classDiagram
    direction LR

    namespace SJH {
        class Shader {
            -GLuint mShaderAddr
            +CreateFromFile()$ ShaderUPtr
            +CreateFromSource()$ ShaderUPtr
            +GetShaderAddr() GLuint
            +~Shader()
        }
        class Program {
            -GLuint mProgramAddr
            +Create(shaders)$ ProgramUPtr
            +CreateWithVSFS(vs, fs)$ ProgramUPtr
            +Use()
            +GetProgramAddr() GLuint
            +~Program()
        }
        class Uniforms {
            <<namespace · free functions>>
            +BuildCache(Program&)
            +Forget(GLuint)
            +SetMat4 / SetVec3 / SetVec4 / SetVec2 / SetFloat / SetInt
            +SetDirLight / SetPointLight / SetSpotLight
        }
    }

    Program ..> Shader : ShaderPtr (attach)
    Uniforms ..> Program : 캐시 key = GLuint

    note for Shader "복사·이동 = delete 명시 (SP1 패치)\n이중 glDeleteShader 방지"
    note for Program "복사·이동 = delete 명시 (SP1 패치)\nUse() / GetProgramAddr() / BuildCache 는 SP2 seam\n→ RenderContext 가 owner 가 될 예정 (doxygen 메모)"
    note for Uniforms "SP2 에서 RenderContext 멤버로 흡수 예정\n자유 함수 → RenderContext 메서드"

```

---

```mermaid
flowchart LR
    subgraph LEGACY["src/engine/shader_program.cpp L58-101 (legacy)"]
        R[Render: 씬 순회 + filter + Draw]
        AD[DefaultShaderProgram::Apply<br/>glUseProgram + view/proj]
        AT[TextureShaderProgram::Apply<br/>glUseProgram + view/proj + Light]
        POLY[가상 Apply 다형성<br/>Default vs Texture]
    end

    subgraph SP2["SP2 RenderContext (저수준, *어떻게*)"]
        S2_USE[UseProgram + bound 추적]
        S2_SET[SetMat4 / SetVec3 / ...<br/>Uniforms 흡수, 캐시]
        S2_LIGHT[SetDirLight/Point/Spot<br/>광원 helper 흡수]
        S2_BEGIN[BeginFrame: ClearBuffer 흡수]
    end

    subgraph SP3["SP3 RenderSystem + 컴포넌트 (*언제·무엇을*)"]
        S3_SYS[RenderSystem 메인 루프]
        S3_MAT[Material 컴포넌트<br/>→ Program/Texture 참조<br/>+ Default/Texture 분기 흡수]
        S3_MESH[Mesh 컴포넌트 + Draw]
        S3_LIGHT[Light 컴포넌트]
        S3_DECIDE[per-frame uniform 결정<br/>view/proj/light 어떤 걸 셋업할지]
    end

    R --> S3_SYS
    R --> S3_MAT
    R --> S3_MESH
    AD --> S2_USE
    AD --> S2_SET
    AD --> S3_DECIDE
    AT --> S2_USE
    AT --> S2_SET
    AT --> S2_LIGHT
    AT --> S3_LIGHT
    AT --> S3_DECIDE
    POLY --> S3_MAT

```

---

```
[ModelBase::Draw  L91-106]
 ├─ L93-95 nullptr 가드 (Material/program 없으면 skip)
 │      → SP3 RenderSystem 의 entity 쿼리
 │        (registry.view<Mesh, Material, Transform>() 로 자동 필터)
 │
 ├─ L97  mMaterial->program->ProgAddr
 │      → SP3 Material 컴포넌트 (Program 리소스 참조, B 결정)
 │      → SP1 Program::GetProgramAddr() 가 핸들 제공
 │      → SP3 RenderSystem 이 RenderContext.UseProgram(program) 호출
 │
 ├─ L100-101  glUniformMatrix4fv(... UNIFORM_MODEL_MAT, transform)
 │      • 메커니즘: SP2 RenderContext::SetMat4
 │      • 결정·계산: SP3 Transform 컴포넌트 + RenderSystem
 │        (mTransform.GetModelMatrix() 가 Transform 컴포넌트로 분리)
 │
 ├─ L102  mMaterial->Apply()
 │      가상 함수 자체는 사라짐. 내용물이 갈라짐:
 │      • BaseColor / 머티리얼 uniform → SP2 SetVec3/4 + SP3 Material 데이터
 │      • 텍스처 바인딩 → SP2 BindTexture + SP3 Material 데이터
 │      → RenderSystem 이 Material 컴포넌트를 *읽어* RenderContext 에 풀어줌
 │
 ├─ L104  glBindVertexArray(mVAOAddr)
 │      • 메커니즘: SP2 RenderContext::BindMesh / 또는 Mesh 리소스의 Bind()
 │      • 핸들 제공: ★ Mesh/Model 리소스 (아래 별도 항목 참조)
 │      • 결정: SP3 Mesh 컴포넌트가 Model 리소스 참조
 │
 └─ L105  glDrawElements(...)
        • 메커니즘: SP2 RenderContext::DrawIndexed
        • 메타데이터: ★ Mesh/Model 리소스 (indexCount, topology)
        • 호출자: SP3 RenderSystem

[BindPhongMaterial  L110-116]
   glActiveTexture + glBindTexture × 2
   • 메커니즘: SP2 RenderContext::BindTexture(unit, textureId)
   • 데이터(어떤 texture, 어떤 unit): SP3 Material 컴포넌트
     (PhongMaterial 의 diffuseUnit/diffuseTexture/specularUnit/specularTexture)
   • 호출자: SP3 RenderSystem 이 Material 보고 unit 수만큼 반복
```

```mermaid
flowchart LR
    subgraph LEGACY["src/engine/model_base.cpp L91-116"]
        G[L93-95 nullptr 가드]
        P[L97 progAddr 추출]
        T[L100-101 model uniform]
        A[L102 Material::Apply]
        V[L104 BindVertexArray]
        D[L105 glDrawElements]
        B[L110-116 BindPhongMaterial<br/>glActiveTexture + glBindTexture]
    end

    subgraph SP1["SP1 (리소스)"]
        S1P[Program::GetProgramAddr]
    end

    subgraph MESHRES["★ Mesh/Model 리소스 (분해 미명시)"]
        M_RAII[VAO/VBO/EBO RAII<br/>Build/Deconstruct]
        M_META[indexCount, topology]
    end

    subgraph SP2["SP2 RenderContext"]
        S2_USE[UseProgram + bound 추적]
        S2_SET[SetMat4 / SetVec / ...]
        S2_TEX[BindTexture unit, id]
        S2_BMESH[BindMesh / DrawIndexed]
    end

    subgraph SP3["SP3 컴포넌트 + RenderSystem"]
        C_MAT[Material 컴포넌트<br/>Program · Texture · BaseColor · units]
        C_MESH[Mesh 컴포넌트<br/>→ Mesh 리소스 참조]
        C_TRANSFORM[Transform 컴포넌트<br/>GetModelMatrix]
        SYS[RenderSystem 루프]
    end

    G --> SYS
    P --> C_MAT
    P --> S1P
    P --> S2_USE
    T --> S2_SET
    T --> C_TRANSFORM
    A --> C_MAT
    A --> S2_SET
    A --> S2_TEX
    V --> S2_BMESH
    V --> C_MESH
    V --> M_RAII
    D --> S2_BMESH
    D --> M_META
    B --> S2_TEX
    B --> C_MAT

```